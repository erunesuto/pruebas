using UnityEngine;

public class PararAudio : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //Audio.audioSource.Pause(true);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
